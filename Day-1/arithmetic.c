#include<stdio.h>
int main()
{
    int a=5,b=20,sum,diff,divi,multi;
    sum=a+b;
    diff=b-a;
    divi=b/a;
    multi=b*a;
    printf("sum = %d", sum);
    printf("\ndiff = %d", diff);
    printf("\ndivi = %d", divi);
    printf("\nmulti = %d", multi);
    return 0;   
}